SELECT
    DISTINCT continent
FROM
    country
ORDER BY
    continent ASC; 
    -- creamos una nueva tabla con los nombres de cada continente y
INSERT INTO
    continent (name)
SELECT
    DISTINCT continent
FROM
    country
ORDER BY
    continent ASC